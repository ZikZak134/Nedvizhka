# Дизайн‑правила и технические рекомендации

## 📁 Название и статус
Файл: **`design_rules.md`**
Тип продукта: **Web‑платформа (Desktop & Mobile)**
Стиль: **Минимальный премиум + выразительные микровзаимодействия**

---

## 1. Цель и планка качества
**Главная цель:** создать «премиальный» опыт, где эстетика усиливает функциональность.
**Критерии приемки (Definition of Done):** каждая фича проверяется по осям Awwwards (40/30/20/10) и Webby:
- **Design (40 %):** визуальное совершенство (типографика, сетка, цвета OKLCH).
- **Usability (30 %):** скорость восприятия, доступность (a11y), навигация.
- **Creativity (20 %):** инновации в UX (View Transitions, scroll‑driven анимации).
- **Content (10 %):** качество данных и копирайтинг (ясность, «голос» бренда).

---

## 2. Визуальный язык и токены (OKLCH)

### Палитра (перцептивное цветовое пространство)
Используем **OKLCH** для генерации палитры – чистые градиенты и предсказуемая контрастность.

```css
:root {
  /* Основные цвета в OKLCH */
  --brand-h: 255;   /* оттенок синего */
  --brand-l: 62%;
  --brand-c: 0.22;
  
  --brand: oklch(var(--brand-l) var(--brand-c) var(--brand-h));
  
  /* Семантические токены */
  --surface-1: oklch(98% 0.01 250);   /* основной фон */
  --surface-2: oklch(95% 0.02 250);   /* фон карточек */
  --surface-3: oklch(90% 0.03 250);   /* границы, разделители */
  
  --on-surface: oklch(20% 0.02 250);  /* основной текст */
  --on-surface-muted: oklch(55% 0.05 250); /* вспомогательный текст */
  
  /* Состояния через color‑mix */
  --action-hover: color-mix(in oklch, var(--brand), white 12%);
  --action-press: color-mix(in oklch, var(--brand), black 12%);
  --intent-danger: oklch(60% 0.2 25);
}
```

### Контейнеризация смысла
Каждый смысловой блок – явный контейнер (Bento‑grid). Отступы **24 px+** создают «премиальный воздух», а крупные блоки сразу бросаются в глаза.

---

## 3. Макет и система компонентов (Grid + Container Queries)

### Container Queries
Компоненты должны быть независимы от глобального viewport. Используем `@container` вместо `@media`.

```css
.card { 
  container: card / inline-size; 
}

@container card (min-width: 400px) {
  .card__content { 
    display: grid; 
    grid-template-columns: 2fr 1fr; 
  }
}
```

### CSS Subgrid для идеального выравнивания
Вложенные элементы (заголовки, цены, кнопки) в разных карточках выравниваются по одной визуальной линии.

```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
}

.card {
  display: grid;
  grid-template-rows: subgrid; /* наследуем строки родителя */
  grid-row: span 3; /* заголовок, контент, футер */
}
```

---

## 4. Анимация и взаимодействие (Magic feel)

### View Transition API
Межстраничные и межсостояний переходы – плавные морфинги.
В SPA (Next.js) используем нативный `document.startViewTransition`.

```javascript
function updateFilters(newFilters) {
  document.startViewTransition(() => {
    applyFiltersDOM(newFilters);
  });
}
```

### Scroll‑driven анимации
Декларативные анимации, привязанные к скроллу, работают в 60 fps.

```css
@keyframes fade-in-up {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.reveal-on-scroll {
  animation: fade-in-up linear both;
  animation-timeline: view();
  animation-range: entry 10% cover 30%;
}
```

### Правила движения
- **Длительность:** 300‑500 ms.
- **Easing:** `cubic-bezier(0.2, 0, 0.2, 1)`.
- **Reduced Motion:** всегда поддерживаем `@media (prefers-reduced-motion: reduce)`.

---

## 5. «Невидимые» улучшения и производительность

### Popover API
Для тултипов, меню и дропдаунов используем нативный HTML‑popover без JS‑библиотек.
```html
<button popovertarget="my-popover">Открыть</button>
<div id="my-popover" popover>Содержимое</div>
```

### Интеллектуальная логика CSS (`:has`)
Управляем состоянием родителя от дочерних элементов.
```css
.card:has(.checkbox:checked) {
  border-color: var(--brand);
  background-color: color-mix(in oklch, var(--brand), transparent 95%);
}
```

### Rendering Performance
Для длинных списков используем `content-visibility`.
```css
.heavy-section {
  content-visibility: auto;
  contain-intrinsic-size: 1px 500px;
}
```

---

## 6. UX‑чек‑лист перед релизом
1. **Иерархия:** главное выделено размером и контрастом.
2. **Контейнеры:** использован Subgrid для выравнивания?
3. **Цвета:** все цвета в OKLCH, без «грязных» смешиваний.
4. **A11y:** контраст текста > 4.5:1.
5. **Моушен:** есть View Transitions? Отключены при `prefers-reduced-motion`?

Эти правила дают техническую базу для создания **World‑Class** интерфейса.
