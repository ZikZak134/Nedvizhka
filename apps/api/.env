PROJECT_NAME="EstateAnalytics"
ENVIRONMENT="prod"
SECRET_KEY="changeme_prod_secret"

# Database (Used by docker-compose)
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=estate_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_secure_password

# Local Storage (Already set in code, but good for reference)
UPLOAD_DIR="uploads"
